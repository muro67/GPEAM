package test.com.aem;

/*import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;*/

public class testSelDBgp1 {

    /*public static void main(String[] args) {
        // TODO Auto-generated method stub

        // Creating a driver object referencing WebDriver interface
        System.setProperty("webdriver.gecko.driver", "c:\\geocoder\\geckodriver.exe");
        WebDriver driver = new FirefoxDriver();

        driver.get("http://localhost/phpmyadmin/");

        // driver.get("http://demo.guru99.com/test/newtours/register.php");

        String baseUrl = "http://localhost/phpmyadmin/";
        driver.get(baseUrl);

        driver.manage().window().maximize();

        driver.findElement(By.xpath("//*[@id='input_username']")).sendKeys("root");
        ;

        // WebElement LastName = driver.findElement(By.id("LastName"));

        // WebElement email = driver.findElement(By.id("email"));

        driver.findElement(By.xpath("//*[@id='select_server']/option[1]")).click();

        driver.findElement(By.xpath("//*[@id='input_username']")).click();

        driver.findElement(By.xpath("//*[@id='input_go']")).click();

        driver.findElement(By.xpath("//*[@id=topmenu\"]/li[1]/a")).click();
    }*/
}
